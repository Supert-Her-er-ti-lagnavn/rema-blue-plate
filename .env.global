# Hugging Face Router Chat App
# Free access to multiple AI models via Hugging Face

# Hugging Face Token (get from https://huggingface.co/settings/tokens)
# Replace 'your_key_here' with your real HF token from the website above
VITE_HF_TOKEN=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

# Default model (can be changed in UI)
VITE_DEFAULT_MODEL=meta-llama/Meta-Llama-3-8B-Instruct

# API Settings
# Hugging Face Router API - should work with proper token permissions
VITE_BASE_URL=https://router.huggingface.co/v1
VITE_DEFAULT_MAX_TOKENS=500
VITE_DEFAULT_TEMPERATURE=0.7